# csvFileHandler.py
# this handles reading urls from csv files
from modules.scrapeURL import scrapeURL

def CSVHandler(file):
    # this needs to work the same way TXTHandler works
    print("ERROR: This mode is not complete yet")